<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#playUrl"
          th:href="@{__${playUrl}__}" th:text="#{game.play}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footerFragment}"/>
    <attr sel="#homeWelcome" th:text="#{game.title}"/>
</thlogic>